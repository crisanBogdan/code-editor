import assert from 'node:assert'
import { describe, it } from 'node:test'
import { handleWebSocket } from '../index'

describe('handleWebSocket', () => {
    describe('on a client join message', () => {
        it('should add it to an existing channel', () => {

        })

        it('should add create a new channel if no channel found by id', () => {

        })

        it('when creating a new channel it should use the payload id if'
            + ' it\'s a valid uuid', () => {

        })
    })
})